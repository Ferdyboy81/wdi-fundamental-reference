var age = 20
var ticketPrice = function setTicketPrice(){
    if(age >= 65)
        {return ticketPrice = "$6.00";}
    if(age < 25) {
        {return ticketPrice = "$8.00";}
    else
        {return ticketPrice = "$10.00"}
    }
log(ticketPrice)
